const assert = require ('assert');

const sum = (num1, num2) => num1 + num2;

const expected = sum (5, 9,);
// assert.strictEqual(expected, 14, 'cinco mais quatro é igual a nove') // verifica se é igual
// assert.notStrictEqual(expected, 14, 'Não  pode ser Igual') // nao pode ser igual a nove ;
// assert.ok(expected === 14, 'um mais dois elementos é igual a 14') // checa se o primeiro argumento  é verdadeiros

// lebrando que o teste de erro so aparece se  o teste falhar;


