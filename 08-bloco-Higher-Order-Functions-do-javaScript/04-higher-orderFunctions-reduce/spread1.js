const array = ['ana', 'joana', 'alberto', 'joaquin', 'luiza' ];

//spread = espalha o array
console.log(...array);

const somatario =(values) => {
  let sum = 0;
  value.forEach(value => sum += value)
  return sum;
}