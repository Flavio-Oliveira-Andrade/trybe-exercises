const pokeTips = callback => {
  // setTimeout  é assicrona
  setTimeout(() => {
    callback('bullassur é o melhor pokemon para começar.');

  }, 2000);


}

test('bullassur é o melhor pokemon para começar.',(done) => {
  expect(data).toBe('bullassur é o melhor pokemon para começar.');
  done();
})

const bySpeed = startterpokemons
  .filter(pokemonsFiltered.speed) >= minimimSpeed
  .map(pokemon => pokemon.name);

if (true){
  resolve(bySpeed)
};

  reject('no pokemon found');

